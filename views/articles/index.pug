extends ../layout

block content
  h1= title
  hr
  for article in articles
    hr

    div.article
      a(href='./' +   article.slug)
        h2= article.title
        div!= article.description
    hr
  script(src='/dist/js/articles.app.min.js')